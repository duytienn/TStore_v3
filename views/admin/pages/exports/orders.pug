extends ../../layouts/default.pug

block main
  style(type="text/css").
    .export-form {
      max-width: 600px;
      margin: 0 auto;
      padding: 20px;
      background-color: #fff;
      border-radius: 8px;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    }
    .form-group {
      margin-bottom: 20px;
    }
    .form-group label {
      display: block;
      margin-bottom: 5px;
      font-weight: 500;
      color: #4b5563;
    }
    .form-control {
      width: 100%;
      padding: 8px 12px;
      border: 1px solid #d1d5db;
      border-radius: 4px;
      font-size: 14px;
    }
    .btn-export {
      background-color: #0891b2;
      color: white;
      padding: 10px 16px;
      border: none;
      border-radius: 4px;
      cursor: pointer;
      font-weight: 500;
      transition: all 0.2s;
    }
    .btn-export:hover {
      background-color: #0e7490;
      transform: translateY(-2px);
    }
    .date-range {
      display: flex;
      gap: 15px;
    }
    .date-range .form-group {
      flex: 1;
    }
    .card-header {
      background-color: #f9fafb;
      padding: 15px 20px;
      border-bottom: 1px solid #e5e7eb;
      border-radius: 8px 8px 0 0;
    }
    .card-header h3 {
      margin: 0;
      color: #111827;
      font-size: 18px;
    }
    .card-body {
      padding: 20px;
    }
    .info-text {
      color: #6b7280;
      font-size: 14px;
      margin-bottom: 20px;
    }

  div.export-form
    div.card-header
      h3 Xuất báo cáo đơn hàng
    
    div.card-body
      div.info-text
        p Chọn khoảng thời gian để xuất báo cáo đơn hàng thành công trong giai đoạn đó. 
        p Báo cáo sẽ bao gồm thông tin chi tiết về sản phẩm đã bán, số lượng, giá bán và tổng doanh thu.
      
      form(action="/admin/exports/orders", method="POST")
        div.date-range
          div.form-group
            label(for="startDate") Ngày bắt đầu
            input.form-control(
              type="date"
              id="startDate"
              name="startDate"
              required
            )
          
          div.form-group
            label(for="endDate") Ngày kết thúc
            input.form-control(
              type="date"
              id="endDate"
              name="endDate"
              required
            )
        
        div.form-group
          button.btn-export(type="submit") 
            i.fas.fa-file-export.mr-2
            | Xuất file Excel